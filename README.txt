creating a list app. The app will create lists of many things (favorite tv shows, movies, comedians...)
existing list you will displayed in alphabetical order in rows.
if an item is added to a list that doesn't exist, the list will be created.

